[package]
name = "burl"
version = "0.1.0"
edition = "2024"
description = "Minimal file-based workflow orchestrator for agentic coding pipelines"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
thiserror = "2"
